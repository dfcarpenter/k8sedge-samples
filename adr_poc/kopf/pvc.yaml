apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{name}"
  annotations:
    volume.beta.kubernetes.io/storage-class: standard 
spec:
  resources:
    requests:
      storage: "{size}"
  accessModes:
    - ReadWriteOnce
